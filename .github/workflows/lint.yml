name: Lint

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: install system deps (Ubuntu / Tauri v2)
        # shikomi-gui は Tauri v2 を使用するため webkit2gtk 等が必要
        # 参照: https://v2.tauri.app/start/prerequisites/#linux
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: install just
        run: cargo install --locked just

      # justfile が CI / local / 手動の 3 経路で単一真実源 (DRY)。
      # workspace.lints.clippy で all="deny", pedantic="warn" が設定済みのため -D warnings は付けない。
      - name: just fmt-check
        run: just fmt-check

      - name: just clippy
        run: just clippy

      - name: shellcheck (scripts/smoke-e2e.sh)
        # 設計根拠: docs/features/shikomi-gui/build-ci/detailed-design/e2e.md §6.4
        run: shellcheck scripts/smoke-e2e.sh
