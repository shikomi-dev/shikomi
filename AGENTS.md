# 作業規則

最初にREADME、[開発の進め方](docs/000_process.md)、対象Issue、変更するコードと関係するシナリオを読みます。履歴はIssueとPR、現在の正本と更新範囲は開発の進め方に従います。

## 設計と実装

- 独立して配備する処理は `backend/<サービス>`、配布する利用先は `client/<製品>` に置き、公開契約で接続する。
- 機能単位で変更を閉じ、業務の概念と言葉を型へ写す。複雑さに必要な分だけDDDと内向きの依存を使う。
- 入口は同じ粒度のメソッドを順に呼び、呼ばれた先で一段詳しくする。状態と依存は責任を持つ型へ閉じる。
- 不正な入力・応答は境界で拒み、結果不明を成功・未保存と断定しない。
- 一つの事実を複数の文書へ写さない。変更の影響がない文書は触らない。

具体的な判断は[設計原則](docs/010_design-principles.md)と[実装構造](docs/020_code-structure.md)を参照します。

## テスト

- 利用者に近い確認を先に設計し、実装と同時にテストを作る。
- Pythonのテストは目的ごとにクラスへまとめる。Goは標準のTest関数とサブテストで同じまとまりを表す。
- 表の各行は独立したテストとして実行する。可変な状態をケース間で共有しない。
- 入力と期待結果はCSV等を正本にし、受入例一覧からクラス・メソッド・ケースを含む実行先へ結ぶ。
- 保存や外部通信の準備・後始末はfixtureへ置く。読み込み器の単体テストは専用の小さなデータを使い、実シナリオの件数や順序を固定しない。
- 受入テストは外から見える結果を判定する。内部の保存方式や構造は別の検査で確認する。

詳細は[テストの考え方](docs/030_testing.md)、完了判定は[完了の判断](docs/040_completion.md)を参照します。

## Gitと成果物

専用worktreeとブランチで作業し、既定ブランチへ直接pushしません。増分はIssue作成後に `INC-<Issue番号>.md` として作ります。テストの番号をIssue作成時に予約しません。

一コミットは一つの論理的変更とし、日本語で理由を書き、`Github-Issue:#<番号>` を添えます。PRには実際の確認結果を記載します。未達・未確認を対象外へ移して完了にしません。明示的な承認なしにマージしません。
