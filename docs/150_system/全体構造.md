# 全体構造

配布単位は `client/shikomi`。端末の受付とGNOME Shell拡張を一緒に配布します。拡張がキー入力、保存、貼り付けを所有し、端末はセッション内D-Busで呼び出します。Webサーバーは不要です。

配布版の正本は `VERSION`。GitHub Releaseの公開を起点にdebを作り、署名したaptの一覧と公開鍵をGitHub Pagesへ配備します。利用者はaptで `/usr/bin/shikomi` とシステムのGNOME拡張を導入します。署名の秘密鍵はGitHub ActionsのSecretだけへ保持します。
