# INC-<Issue番号> 今回変えること

対応Issue: #<Issue番号>

## 変更範囲

今回追加・変更・削除する体験と対象外を短く書く。既存のシナリオ全文を写さない。

## 設計と確認

どの責任に変更を閉じ、なぜその形にするか。その判断を外の入口と境界でどう確かめるかを対に書く。自明な設計は一行でよい。

具体的な入力と期待結果はテストデータ・手動確認書に置き、作業中はその実行先を参照する。

## 結果のありか

対応PRを指す。完了後の実装はそのPRのコミットから確認し、未来のテスト名やケース変更へこの記録を追従させない。
